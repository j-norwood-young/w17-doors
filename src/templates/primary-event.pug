.primary-event.container.yellow-bg.left
    h2 Now
    h3= event.name
    h4 #{moment(event.starttime).format("HH[h]mm")} - #{moment(event.endtime).format("HH[h]mm")} (Ends #{moment(event.endtime).fromNow()})
    
